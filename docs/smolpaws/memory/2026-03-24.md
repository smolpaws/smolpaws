# 2026-03-24

- SmolPaws now treats `~/repos/smolpaws/docs/smolpaws/` as the canonical local source of self/context.
- The local GitHub path is live end-to-end through Cloudflare Worker -> queue -> local agent-server -> GitHub reply.
- The canonical local runtime secret file is `~/.smolpaws/.env`.
